# Route-B Correspondence Tests: HyStart++ (T48)

> 🔬 *Generated by Lean Squad automated formal verification.*

## Summary

This directory contains **Route-B executable correspondence tests** that
verify the Lean model in `FVSquad/Hystart.lean` faithfully represents
the pure-computation functions from
`quiche/src/recovery/congestion/hystart.rs`.

**Result**: ✅ **27/27 cases PASS** — Lean model agrees with Rust on all inputs.

---

## What Is Tested

| Target | T48 — HyStart++ RTT threshold clamp + CSS divisor |
|--------|-----------------------------------------------------|
| Rust source | `quiche/src/recovery/congestion/hystart.rs` |
| Lean model | `formal-verification/lean/FVSquad/Hystart.lean` |
| Test count | 27 cases |

### Functions tested

| Function | Cases | Description |
|----------|-------|-------------|
| `rtt_thresh(last_ms)` | 19 | RTT threshold clamp: `min(max(last/8, 4ms), 16ms)` |
| `css_cwnd_inc(pkt_size)` | 8 | CSS cwnd increment: `pkt_size / 4` |

### Coverage

**`rtt_thresh`** cases cover all three clamping regions:
- Below minimum (6 cases): `last_ms/8 < MIN_RTT_THRESH (4ms)` → output = 4
- In range (6 cases): `last_ms/8 ∈ [4, 16]` → output = `last_ms/8`
- Above maximum (7 cases): `last_ms/8 > MAX_RTT_THRESH (16ms)` → output = 16

**`css_cwnd_inc`** cases cover:
- Zero and sub-divisor inputs (3 cases)
- Exact divisor (1 case)
- Typical packet sizes: 100, 1200, 1448, 9000 bytes

---

## How to Run

```bash
cd /path/to/quiche
rustc formal-verification/tests/hystart/hystart_test.rs -o /tmp/hystart_test
/tmp/hystart_test
```

No Cargo workspace required — single-file, no dependencies.

---

## Correspondence Mapping

| Lean definition | Rust source | Notes |
|-----------------|-------------|-------|
| `MIN_RTT_THRESH = 4` | `hystart.rs:40: Duration::from_millis(4)` | Nat (ms) vs Duration |
| `MAX_RTT_THRESH = 16` | `hystart.rs:42: Duration::from_millis(16)` | Nat (ms) vs Duration |
| `CSS_GROWTH_DIVISOR = 4` | `hystart.rs:44: const CSS_GROWTH_DIVISOR: usize = 4` | exact |
| `rtt_thresh last_ms` | `on_packet_acked` lines 143-145: `max(last/8, MIN).min(MAX)` | Nat (ms) model |
| `css_cwnd_inc pkt_size` | `Hystart::css_cwnd_inc` line 191-193: `pkt_size / CSS_GROWTH_DIVISOR` | exact |

**Abstracted**: stateful HyStart++ logic (RTT sampling, CSS round counting,
window_end tracking) is not modelled here — the tests cover only the
pure-computation functions verified in Hystart.lean.

---

## Known Limitations

- Duration (f64/ns) arithmetic is modelled as integer milliseconds; sub-ms
  rounding is not captured (not relevant for these constants).
- The stateful `on_packet_acked`, `start_round`, and `congestion_event`
  methods are not tested here.
