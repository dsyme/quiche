# Route-B Correspondence Tests: `StreamStateMachine` (T44)

> 🔬 *Generated by Lean Squad automated formal verification.*

## Summary

This directory contains **Route-B executable correspondence tests** that
verify the Lean model in `FVSquad/StreamStateMachine.lean` faithfully represents
the Rust stream-completion and writability predicates from:
- `quiche/src/stream/mod.rs` — `Stream::is_complete`, `Stream::is_writable`
- `quiche/src/stream/send_buf.rs` — `SendBuf::is_fin`, `is_complete`, `is_shutdown`
- `quiche/src/stream/recv_buf.rs` — `RecvBuf::is_fin`

**Result**: ✅ **46/46 cases PASS** — Lean model agrees with Rust on all inputs.

---

## What Is Tested

| Target | T44 — QUIC stream state machine (RFC 9000 §3) |
|--------|-----------------------------------------------|
| Rust source | `quiche/src/stream/{mod.rs,send_buf.rs,recv_buf.rs}` |
| Lean model | `formal-verification/lean/FVSquad/StreamStateMachine.lean` |
| Test count | 46 cases |

### Predicates tested

| Predicate | Cases | Description |
|-----------|-------|-------------|
| `RecvBuf::is_fin` | 5 | All combinations of fin_off/off |
| `SendBuf::is_fin` | 4 | fin_off set/unset, off at/before fin |
| `SendBuf::is_complete` | 5 | acked_end equal/less/absent vs fin_off |
| `SendBuf::is_shutdown` | 2 | shutdown flag true/false |
| `Stream::is_writable` | 5 | shutdown/fin/fc combinations |
| `Stream::is_complete` bidi | 4 | all combinations of recv-fin × send-complete |
| `Stream::is_complete` uni | 4 | local uni and remote uni |
| Complete stream not writable | 2 | bidi and local-uni invariants |
| Edge cases | 9 | large offsets, zero-length, shutdown+fin, acked_end > fin |
| Stream ID classification | 6 | stream IDs 0–5, bidi/local flags |

### Key invariants verified

```
shutdown  ⇒  !writable
sendIsFin ⇒  !writable
sendIsComplete ∧ WFStreamSend ⇒ sendIsFin ⇒ !writable
bidi_complete  ⇒  !writable  (via WF)
```

---

## Files

| File | Purpose |
|------|---------|
| `stream_state_machine_test.rs` | Rust harness: inline Rust predicates + Lean model transliteration |
| `README.md` | This file |

---

## How to Run

```bash
cd formal-verification/tests/stream_state_machine
rustc stream_state_machine_test.rs -o /tmp/ssm_test && /tmp/ssm_test
```

Expected output: all 46 cases `PASS`, exit code 0.

---

## Last Verified Run

- **Date**: 2026-05-02 UTC
- **Lean version**: 4.29.0 (lean-toolchain)
- **Rust**: system rustc (stable)
- **Cases**: 46 total, all PASS

### Test Output (excerpt)

```
# Results: 46/46 PASS, 0 FAIL
```

---

## Coverage

| Category | Cases | Description |
|----------|-------|-------------|
| `RecvBuf::is_fin` | 5 | All cases: no fin_off, exact, partial, zero-length, not started |
| `SendBuf::is_fin` | 4 | No fin_off, exact, partial, zero-length |
| `SendBuf::is_complete` | 5 | No fin_off, all acked, partial, zero-length, none acked |
| `SendBuf::is_shutdown` | 2 | Shutdown true/false |
| `Stream::is_writable` | 5 | Normal, shutdown, fin, fc=false, shutdown+fin |
| `Stream::is_complete` bidi | 4 | Both done, recv only, send only, neither |
| `Stream::is_complete` uni | 4 | Local uni complete/incomplete, remote uni complete/incomplete |
| Complete ⇒ not writable | 2 | bidi and local-uni invariants |
| Edge cases | 9 | Large offsets, zero-length bidi, shutdown+fin, acked>fin |
| Stream ID classification | 6 | IDs 0–5 (client/server bidi/uni) |

What is **not** covered: the full `Stream` struct (receive buffer contents,
flow-control fields, PMTUD), multi-stream interaction, or the full QUIC
connection state. The Lean model explicitly documents these omissions.
