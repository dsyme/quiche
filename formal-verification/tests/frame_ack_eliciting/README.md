# Route-B Correspondence Tests: `FrameAckEliciting` (T42)

> 🔬 *Generated by Lean Squad automated formal verification.*

## Summary

This directory contains **Route-B executable correspondence tests** that verify
the Lean model in `FVSquad/FrameAckEliciting.lean` faithfully represents the
Rust `Frame::ack_eliciting` and `Frame::probing` predicates from
`quiche/src/frame.rs`.

**Result**: ✅ **33/33 cases PASS** — Lean model agrees with Rust on all inputs.

---

## What Is Tested

| Target | T42 — `Frame::ack_eliciting` / `Frame::probing` |
|--------|-------------------------------------------------|
| Rust source | `quiche/src/frame.rs` lines 814–833 |
| Lean model | `formal-verification/lean/FVSquad/FrameAckEliciting.lean` |
| Test count | 33 cases (23 per-variant + 10 property checks) |

### Predicates tested

| Predicate | Cases | Description |
|-----------|-------|-------------|
| `Frame::ack_eliciting` | 23 | True for all frame kinds except Padding/ACK/ConnectionClose/ApplicationClose |
| `Frame::probing` | 23 | True only for Padding/NewConnectionId/PathChallenge/PathResponse |
| `ackEliciting_false_iff` | 1 | Non-eliciting set is exactly {Padding, ACK, ConnClose, AppClose} |
| `probing_true_iff` | 1 | Probing set is exactly {Padding, NewConnId, PathChallenge, PathResponse} |
| Various property theorems | 8 | Count invariants, specific kind checks |

---

## How to Run

```bash
rustc frame_ack_eliciting_test.rs -o /tmp/fae_test && /tmp/fae_test
```

No external dependencies — compiles with stable Rust.

---

## Correspondence Notes

The Lean `FrameKind` enum has 23 variants; the Rust `Frame` enum has 26 (it
includes `CryptoHeader`, `StreamHeader`, and `DatagramHeader` in addition to
the 23 in the Lean model). These three additional Rust variants behave
identically to `Crypto`, `Stream`, and `Datagram` for both predicates — they
are not matched by either `!matches!` pattern — so the Lean model is a sound
abstraction of the full Rust enum.

> 🔬 *Lean Squad — automated formal verification for `dsyme/quiche`.*
