# Route-B Correspondence Tests: `PathState` (T38)

> 🔬 *Generated by Lean Squad automated formal verification.*

## Summary

This directory contains **Route-B executable correspondence tests** that
verify the Lean model in `FVSquad/PathState.lean` faithfully represents
the Rust `PathState` enum and its transition operations from
`quiche/src/path.rs`.

**Result**: ✅ **75/75 cases PASS** — Lean model agrees with Rust on all inputs.

---

## What Is Tested

| Target | T38 — PathState monotone progression (RFC 9000 §8.2) |
|--------|-------------------------------------------------------|
| Rust source | `quiche/src/path.rs:53-460` (`PathState`, `promote_to`, `on_challenge_sent`, `on_response_received`, `on_failed_validation`) |
| Lean model | `formal-verification/lean/FVSquad/PathState.lean` |
| Test count | 75 cases |

### Operations tested

| Operation | Cases | Description |
|-----------|-------|-------------|
| `promote_to(current, new)` | 25 | All 5×5 (current, new) pairs |
| `on_challenge_sent(s)` | 5 | All 5 states |
| `on_response_received(s, mtu_ok)` | 10 | All 5 states × 2 mtu_ok values |
| `on_failed_validation()` | 1 | Single fixed result (→ Failed) |
| `working(s)` | 5 | All 5 states |
| State-transition sequences | 4 | Full normal path, MTU-only, failed path, multi-step |
| `promote_to` idempotency | 25 | All 5×5 pairs: `promote_to(promote_to(s,t),t) == promote_to(s,t)` |

### Key state-machine sequences verified

```
Unknown → on_challenge_sent → Validating ✅
Validating → on_response_received(mtu=true) → Validated ✅
Validating → on_response_received(mtu=false) → ValidatingMTU ✅
ValidatingMTU → on_failed_validation → Failed ✅
```

---

## Files

| File | Purpose |
|------|---------|
| `path_state_test.rs` | Rust harness: inline Rust `PathState` + Lean model transliteration |
| `README.md` | This file |

---

## How to Run

```bash
cd formal-verification/tests/path_state
rustc path_state_test.rs -o /tmp/path_state_test && /tmp/path_state_test
```

Expected output: all 75 cases `PASS`, exit code 0.

---

## Last Verified Run

- **Date**: 2026-04-30 UTC
- **Lean version**: 4.30.0-rc2 (elan)
- **Rust**: system rustc (stable)
- **Cases**: 75 total, all PASS

### Test Output

```
# PathState Route-B Correspondence Test
# Source:   quiche/src/path.rs (PathState, promote_to, on_challenge_sent, etc.)
# Lean model: FVSquad/PathState.lean
#
# idx,operation,rust_result,lean_result,status,description
...
75,promote_to idempotent,Validated,Validated,PASS,"idempotency"
#
# Results: 75/75 PASS, 0 FAIL
```

---

## Coverage

| Category | Cases | Description |
|----------|-------|-------------|
| `promote_to` all pairs | 25 | Exhaustive: all 5×5 (current, new) combinations |
| `on_challenge_sent` | 5 | All 5 starting states |
| `on_response_received` | 10 | All 5 states × mtu_ok ∈ {true, false} |
| `on_failed_validation` | 1 | Single result (always → Failed) |
| `working` predicate | 5 | All 5 states |
| Transition sequences | 4 | Normal path, MTU-only, failure, multi-step |
| Idempotency | 25 | `promote_to(promote_to(s,t),t) = promote_to(s,t)` for all pairs |

What is **not** covered: the full `Path` struct fields (timers, challenge queues,
PMTUD), `on_challenge_received`, and multi-path coordination. The Lean model
explicitly documents these omissions. The correspondence tests cover exactly the
pure state-machine fragment that the Lean proofs formalise.
